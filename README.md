Built an NLP classifier comparing 4 ML algorithms (Logistic Regression, Naive Bayes, Random Forest, Gradient Boosting), achieving 96% accuracy on a 1,000-article dataset.
Implemented full text preprocessing pipeline — tokenization, stopword removal, Porter stemming — with TF-IDF (10,000 features, bigrams) for feature extraction.
